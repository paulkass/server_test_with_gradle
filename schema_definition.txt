create keyspace entry_keyspace WITH replication = {'class': 'SimpleStrategy', 'replication_factor': 1};

describe keyspaces;

create table entry_keyspace.entries_table_public ( entry_id timeuuid, body text, title text, primary key (entry_id) );
